.update-time
  = Time.zone.now.strftime('%h %m')

.metrics
  - @developer.each do |dev|
    = render "developer_metrics", developer: dev

.updateable
  .new-dev-offer
    = link_to 'Add Developer', new_developer_path
